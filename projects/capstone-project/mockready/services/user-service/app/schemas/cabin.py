"""""
schema/cabin.py

This is not a database model.
They define what an API accepts and returns

Rule:
    schema/ ->what the API sees(paydentic)
    model/  ->what the db sees(SQlAlschemy)  <- phase 2

"""""